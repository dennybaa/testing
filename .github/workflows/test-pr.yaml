name: test-pr

on:
  pull_request:
    types: [edited]
    branches: []

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: test
      run: |-
        echo '--------------------'
        echo '${{ toJSON(github.event) }}'
