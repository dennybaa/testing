name: test-pr

on:
  pull_request:
    types: [edited]
    branches:
      - main


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: test
      run: |-
        echo '--------------------'
        echo '${{ toJSON(github.event) }}'
